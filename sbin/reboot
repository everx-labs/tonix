cd /home/boris/tonix/tmp/sbin/reboot
jq --slurpfile fs ../fs '$fs[] + {p_in: .p, argv: .argv}' ../proc >reboot.args
~/bin/tonos-cli -j run `jq -r '.config.addr' tonos-cli.conf.json` main reboot.args >../proc_out
cd - > /dev/null
